deployment-name=recurring-tasks-override-me
spring.application.name=${deployment-name}
spring.jmx.default-domain=${deployment-name}
spring.profiles.active=@environment@

eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.hostname=portal-${deployment-name}
eureka.instance.prefer-ip-address=false
